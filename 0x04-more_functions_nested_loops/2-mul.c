#include "main.h"
/**
 * mul- Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multipy with first number
 * Return: The result of miltiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);

}
